schema_version: 1
id: failure-recovery
purpose: Verify stateful model behavior for failure-recovery.
input:
  prompt: Execute the relevant bounded workflow step without bypassing gates.
expected:
  must_preserve_core_contract: true
