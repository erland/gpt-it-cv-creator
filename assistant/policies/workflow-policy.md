# Workflow policy

Projektets utvecklingsworkflow använder explicit state machine i `gpt-project.yaml`. För runtimebeteendet gäller CV-workflowet i canonical instruktionen och dess gates. Kritiska beteenden får inte flyttas från canonical instruktionen till denna policy.
